// Assignment operator (=)
let a = 10; // it will assign 10 to a variable

// Condition operator
// == check equality with value only
const b = '10';
console.log('is a == b ', a == b);

// === check equality with value and type
console.log('is a === b', a === b);

// >, >=, >==
// <, <=, <==

// ! Not
let isMale = true;
console.log('is not a female', !isMale);

// !=, !==,

// join operator
// || , &&
//????????